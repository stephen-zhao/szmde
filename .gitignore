.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Scratch / diagnostic files and test coverage
.tmp-*
/coverage

# Cloud OAuth client config — user's real client_id/secret, never committed
# (lives in the OS app-config dir at runtime; this guards stray repo copies).
gdrive_client.json
